# Python Backend

A persistent Python REPL.  Any valid Python code can be executed.

## Capabilities

- Full Python language: variables, functions, classes, control flow
- Standard library imports available
- State (imports, variables, classes, functions) persists across calls

## Use

Call the `execute` tool with any valid Python code as the argument.
The code is evaluated line by line in a persistent session.
